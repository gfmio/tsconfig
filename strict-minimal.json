{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    // Minimal strict configuration for gradual TypeScript adoption
    // Use this when migrating JS projects to TS or when full strict mode is too aggressive

    // Basic setup
    "lib": ["ESNext"],
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "moduleDetection": "force",

    // Module settings
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "resolvePackageJsonExports": true,
    "resolvePackageJsonImports": true,

    // Minimal strictness (easier migration path)
    "strict": false,
    "noImplicitAny": false,
    "strictNullChecks": false,
    "allowJs": true,
    "checkJs": false,

    // Essential safety checks (always recommended)
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "noEmit": true,

    // Output
    "pretty": true
  }
}
